---
name: edit-existing-office-file
description: Safely edit an existing DOCX, XLSX, or PPTX file.
---

# Edit an Existing Office File

Inspect with `get` or `query` before mutation. Identify stable paths, apply the smallest requested `set`, `add`, `remove`, `move`, or `copy` operations, then validate and render. Preserve unrelated document content and structure.
