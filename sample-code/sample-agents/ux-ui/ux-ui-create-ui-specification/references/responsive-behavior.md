# Responsive behavior

## Use when

Use this variant when layout and interaction change across available space.

## Task guidance

For **Create UI Specification**, define behavior at content-driven thresholds, reflow, truncation, priority, alternate controls, and test viewports.

## Decision checks

- Confirm user goal, state coverage, accessibility impact, and validation evidence.
- Resolve conflicts across flow, content, layout, and platform conventions.
- If multiple references apply, reconcile their constraints explicitly instead of merging incompatible advice.
