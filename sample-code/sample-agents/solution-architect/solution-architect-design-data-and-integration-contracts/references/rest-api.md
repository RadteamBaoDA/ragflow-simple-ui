# REST API contract

## Use when

Use this variant when request-response HTTP integration is selected.

## Task guidance

For **Design Data and Integration Contracts**, define resource semantics, methods, status codes, validation, pagination, idempotency, errors, authentication, and versioning.

## Decision checks

- Confirm drivers, alternatives, trade-offs, failure modes, and measurable constraints.
- Identify which decisions need an ADR or accountable approval.
- If multiple references apply, reconcile their constraints explicitly instead of merging incompatible advice.
